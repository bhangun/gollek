#!/bin/sh
# Maven Wrapper Script
MAVEN_VERSION=3.9.5

if command -v mvn >/dev/null 2>&1; then
    exec mvn "$@"
else
    echo "Maven not found. Please install Maven $MAVEN_VERSION or higher"
    echo "Visit: https://maven.apache.org/download.cgi"
    exit 1
fi
