package tech.kayys.gollek.safetensor.engine.planning;

import tech.kayys.gollek.spi.inference.InferenceRequest;
import java.nio.file.Path;

public class InferenceModelPathResolver {
    public Path resolve(InferenceRequest request, Object config) { return null; }
    private Path resolveModelPath(String modelId, Object config) { return null; }
}
